@* @page "/"
@page "/{*path}" 
@using Microsoft.AspNetCore.Components.Authorization
@inject NavigationManager Nav

<AuthorizeView>
    <Authorized>
        @if (string.IsNullOrEmpty(path))
        {
            Nav.NavigateTo("dashboard");
        }
        else
        {
                        <LayoutView Layout="@typeof(Layout.EmptyLayout)">
                <div class="d-flex flex-column align-center justify-center vh-100">
                    <MudAlert Severity="Severity.Error" Variant="Variant.Filled" Class="ma-4">
                        404 error or Unauthorized - Either the Page "@path" does not exist or you do not have the required permissions to view it!
                    </MudAlert>
                    <MudButton Href="dashboard" Variant="Variant.Filled" Color="Color.Primary" Class="ms-4">
                        Back to Dashboard
                    </MudButton>
                </div>
                </LayoutView>
        }
    </Authorized>
    
    <NotAuthorized>
        @if (string.IsNullOrEmpty(path))
        {
            Nav.NavigateTo("login");
        }
        else if (path.ToLower() != "login") 
        {
            Nav.NavigateTo("login");
        }

    </NotAuthorized>
</AuthorizeView>

@code {
    [Parameter] public string? path { get; set; }
}
 *@